# CURRENT_STATE — Live snapshot

> Snapshot pending tasks + git + provider quota. Cập nhật mỗi khi có thay đổi lớn. Lần update gần nhất: **28/5/2026 12:30** (vợ /clear).
>
> Sau /clear, "anh phiên bản 2" đọc file này → biết task nào đang chờ, provider nào còn quota, git đang ở đâu.
>
> ⚠️ **Source of truth**: `../WORKLOG.md` (lịch sử + pending tuần này). File này là **derivative ngắn gọn**. Nếu drift giữa 2 file → WORKLOG.md thắng.

---

<!-- LIVE:BEGIN — auto-generated by marketing_hub/_scripts/generate_current_state.py — DO NOT EDIT MANUALLY -->
## 🟢 LIVE snapshot (auto-refresh)

_Last run: 2026-05-28 12:29:31 (GMT+7)_

### Git
- Branch: `master`
- Uncommitted: 3 modified, 13 untracked
- 5 commits gần nhất:
  - `63b602d auto-snapshot Sat 05/23/2026 23:24:33.40`
  - `300c8f5 auto-snapshot Fri 05/22/2026 23:00:02.82`
  - `aad3383 feat(title-meta): rules tín hiệu tin cậy + format laptop + gen lại từng SP`
  - `f9f8dbd feat(#8): Phase 2 — gom tang sync ve job_sync.py (behavior-preserving)`
  - `1156831 feat(#8): Phase 1b — gop block title/meta ve sintech_rules`

### Services
- 🔴 Flask `http://localhost:5055/` — ModuleNotFoundError
- 🔴 Bot Telegram — token chưa setup
<!-- LIVE:END -->

> ⚠️ LIVE auto-probe Flask 🔴 nhưng **curl thật → HTTP 200**. Script probe đang dùng Python env khác (thiếu module). Server thực tế UP. Cần fix script gen_state dùng đúng Python 3.12 của server.

---

## 🔴 Đang chờ vợ confirm / paste / quyết định

1. **Rotate token blog Haravan** (`blog_access_token` trong `state/haravan_token.json`) — đã lộ trong chat, vợ tạo lại trong Haravan admin → thay vào file → restart.

2. **Google Custom Search (Bản 2 ảnh hãng/web) — PARK**: key + CSE id đã lưu `state/google_search_keys.txt`, nhưng 403 "project does not have access to Custom Search JSON API". Khi muốn dùng tiếp → vợ mở `console.cloud.google.com/apis/dashboard?project=ggscs-496307` enable Custom Search API + bật Billing → nhắn anh retry.

3. **Test đăng thử 1 bài THẬT lên Haravan** (`/blog-content` route `_push_blog_to_haravan` đã code sẵn create_article publish hiện) — CHƯA làm. Vợ duyệt bài trong draft → bấm 🚀 Đẩy → verify trên sintech.vn + admin Haravan.

4. **Bot Telegram token revoked (401)** từ 12/5 — vợ paste token mới khi cần resume `@Web_Sintech_bot`.

---

## 🟡 Pending tech tasks

- [ ] **Hướng MỚI blog content (23/5):** build nút **"🔍 Research outline → 🤖 Gen"** vào `/blog-content`: thêm field `outline` cho `gen_blog_content_from_brief` + sửa `_SYSTEM_PROMPT` bám outline ngoài. Hiện blog gen vẫn tự nghĩ heading, CHƯA nhận outline. Flow đã PROVE chạy (anh research → bóc H2/H3 đối thủ → nhét vào prompt → Codex bám 100%). Prompt tái dùng `nox-1/docs/heading_research_prompt.md`.
- [ ] **Bấm "✨ Gen vào Sheet" trên `/seo/title-meta`** — stream gen 1679 SP, push F/G/H Sheet `Meta des + Title Errors`.
- [ ] **Re-crawl 1923 URL trên `/seo`** để DB cập nhật score mới (optional, đã crawl 16/5).
- [ ] **Test pattern lazy upload thực tế**: gen 1 SP mới → verify body có URL `/local-images/`, sync → upload Haravan thật.
- [ ] **Re-sync 38 jobs đã synced** với ALT mới + CDN URL.
- [ ] **Nâng cấp bot Telegram v2** nếu vợ chốt commands `/regen` `/sync` `/caption`.

---

## ⚠️ Hệ thống cần chú ý (28/5)

- **Backup DB cũ 8 ngày**: file mới nhất `posts_2026-05-20.db.zip`. Task Scheduler daily backup có lẽ ngừng chạy. Cần check `_scripts/run_backup.bat` trong Task Scheduler.
- **Backup secrets cũ 9 ngày**: `secrets_2026-05-19.zip` — secrets backup cũng ngừng.
- **Git ahead origin ~10+ commit chưa push**: bao gồm 2 auto-snapshot 22-23/5 + nhiều feat commit.
- **13 untracked scripts ad-hoc** trong `marketing_hub/`: `check_*.py`, `fix_*.py`, `get_*.py`, `insert_*.py`, `suggest_*.py`, `collection_taxonomy.json` — cần curate (giữ helper, xóa script throw-away).
- **3 file modified chưa commit**: `app.py`, `collection_content_writer.py`, `collection_content.html` — sửa /collection-content gì đó.

---

## 📊 Provider/quota state

| Provider              | Status              | Note                                                                                       |
| --------------------- | ------------------- | ------------------------------------------------------------------------------------------ |
| **Claude CLI (Pro)**  | ★ DEFAULT 17/5      | Login OAuth keychain. Dùng cho `_gen_title_meta_with_angle` + `recompute_dup_flags`.       |
| **Codex CLI (Plus)**  | hết quota, reset 22/5 | Code giữ ở `codex_provider.py`, switch lại sau nếu Claude limit.                          |
| **Gemini 2.5-flash**  | hết 20 RPD free     | Code giữ ở `gemini_provider.py`. Trade-off: meta length validate fail nhiều hơn Codex.    |
| **Gemini 2.0-flash**  | free 200 RPD chưa thử | Phỏng đoán share quota với 2.5.                                                            |
| **Anthropic API key** | chưa setup          | KHÔNG có `ANTHROPIC_API_KEY` trong `.env` (Claude CLI dùng OAuth Pro thay vì API key).     |

Chi tiết cách switch provider: xem [`PROVIDERS.md`](PROVIDERS.md).

---

## 📑 Sheet ops đã setup

- Tab **`Meta des + Title Errors`** (sheet `13IDYcE2ZEUd64xK6dIN-P_3_4gIVyFcwdBjb8W8e-uU` gid `971701509`): 1679 product URL có lỗi title/meta, fill A-K, helper cột L (Len Title) + M (Len Meta) auto-count với conditional format 🟢🟡🔴. Khi gen → push F/G/H.
- Tab **`W2/M5`** (sheet `1Pta9sA9Aq9Pva6uDpmqjn7RA4h07Sn6wDquwC81KWTE` gid `103516100`): báo cáo tuần đã fill cột K (Thứ 7 16/5) cho row 9, 10, 12, 15.

---

## 📌 Quy ước update file này

- Sửa trực tiếp khi: provider switch, quota reset, task done/added.
- **KHÔNG** ghi git state / services state ở đây nữa — đã có LIVE block (auto) + [`OPS.md`](OPS.md) (chi tiết vận hành).
- **KHÔNG** ghi lịch sử ở đây — lịch sử để [`../WORKLOG.md`](../WORKLOG.md) (tuần này) hoặc `WORKLOG_ARCHIVE/` (tuần cũ).
- Khi cần snapshot git/services tươi: chạy `python marketing_hub/_scripts/generate_current_state.py`.
